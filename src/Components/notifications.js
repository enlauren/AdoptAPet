import React from "react";
import classes from "./notifications.css";

const Notifications = props => {
  return (
    <div className={classes.notifications}>this is the navigation bar</div>
  );
};

export default Notifications;

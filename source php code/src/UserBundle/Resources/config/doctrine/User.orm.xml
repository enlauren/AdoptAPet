<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                   https://raw.github.com/doctrine/doctrine2/master/doctrine-mapping.xsd">

    <entity name="App\UserBundle\Entity\User"
            table="users"
            repository-class="App\UserBundle\Entity\Repository\UserRepository"
    >
        <id name="id" type="integer" column="id">
            <generator strategy="AUTO"/>
        </id>

        <field name="username" column="username" type="string" nullable="true"/>
        <field name="email" column="email" type="string" nullable="false"/>
        <field name="code" column="code" type="string" nullable="true"/>

        <field name="active" column="active" type="boolean" nullable="false">
            <options>
                <option name="default">1</option>
            </options>
        </field>

        <field name="isAdmin" column="is_admin" type="boolean" nullable="false">
            <options>
                <option name="default">0</option>
            </options>
        </field>

        <field name="rememberToken" column="remember_token" type="string" length="100" nullable="true"/>

        <field name="phone" column="phone" type="string" nullable="true" length="100"/>

        <field name="salt" column="salt" type="string" length="100"/>
        <field name="password" column="password" type="string" length="100"/>

        <one-to-many target-entity="App\AppBundle\Entity\Message" mapped-by="author" field="messages"/>
        <one-to-many target-entity="App\AppBundle\Entity\Classified" mapped-by="user" field="classifieds"
                     fetch="EXTRA_LAZY">
            <cascade>
                <cascade-persist/>
                <cascade-remove/>
            </cascade>
        </one-to-many>
        <one-to-many target-entity="App\AppBundle\Entity\Feedback" mapped-by="user" field="feedbacks">
            <cascade>
                <cascade-remove/>
            </cascade>
        </one-to-many>
        <one-to-many target-entity="App\AppBundle\Entity\Cabinet" mapped-by="user" field="cabinete"/>

        <unique-constraints>
            <unique-constraint name="users_email_unique" columns="email"/>
        </unique-constraints>

        <field name="createdAt" column="created_at" type="datetime" nullable="false"/>
        <field name="updatedAt" column="updated_at" type="datetime" nullable="false"/>
        <field name="deletedAt" column="deleted_at" type="datetime" nullable="true"/>

        <lifecycle-callbacks>
            <lifecycle-callback type="prePersist" method="updatedTimestamps"/>
            <lifecycle-callback type="preUpdate" method="updatedTimestamps"/>
        </lifecycle-callbacks>
    </entity>
</doctrine-mapping>
